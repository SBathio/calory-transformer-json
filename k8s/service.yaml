apiVersion: v1
kind: Service
metadata:
  name: calory-transformer-service
spec:
  type: LoadBalancer
  selector:
    app: calory-transformer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080